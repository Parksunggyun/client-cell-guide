# Project Workflow & Convention Guide

```dart
본 문서는 **Notion + GitHub** 협업을 위한 **클라이언트셀**의 Flutter App 개발 표준 가이드입니다. 
릴리즈 노트 생성(Release Drafter)이 구축되어 있으므로 아래 규칙을 준수해 주세요. 
```

---

## **1. 🏗 Work Environments (배포 환경)**

3단계 환경을 통해 안정적인 서비스를 배포 및 운영

1. **develop (개발계):** 개별 기능(Feature)이 머지되는 곳
2. **active (검증계):** QA를 위해 `develop` 에서 머지됨
    - ⚠️ **active 배포 시 `pubspec.yaml` 버전 업 필수**
3. **main (운영계):** `main` 브랜치. `active`에서 검증이 끝난 코드 배포
    - ~~🚀 **Git Tag가 자동 생성되며 Jenkins가 배포**~~

---

## **2. 🎫 Issue & Naming**

모든 작업은 Notion 티켓에서 시작.

**Backlog ID(예: TASK-102)** 가 기준.

### **✅ Issue Naming**

Format: [Type] Backlog-ID: 작업 제목 

Example: [Feat] TASK-102: 소셜 로그인 구현

| **Type** | **설명** |
| --- | --- |
| `[Feat]` | 새로운 기능 추가 |
| `[Fix]` | 버그 수정 |
| `[Design]` | UI/UX 디자인 작업 |
| `[Refactor]` | 코드 리팩토링 (기능 변경 없음) |
| `[Chore]` | 빌드 설정, 패키지 매니저 등 기타 작업 |

### **🌿 Branch Naming**

이슈 제목과 매핑되는 브랜치 생성. (소문자, kebab-case)

> Format: type/Backlog-ID-description
> 
- `feat/TASK-102-social-login`
- `fix/TASK-105-payment-404-error`
- `design/TASK-110-main-banner`

---

## **3. 🚀 Pull Request**

PR 템플릿에 따라 내용을 작성하고 **Label을 부착 必**

### **📝 PR Rules**

1. **Title:** 이슈 제목과 **100% 동일하게** 작성. (릴리즈 노트에 그대로 반영됨)
    - 예: `[Feat] TASK-102: 소셜 로그인 구현`
2. **Labels (필수):** 우측 사이드바에서 라벨 선택.
    - `Type: Feature`, `Type: Bug` 등 -> **자동 분류의 기준**.
    - 디자인 작업이 포함된 경우 `Area: Design` 라벨 추가.

### **🔄 Environment Merge Rule (배포 PR)**

환경 간 이동 시에는 이슈를 생성하지 않고 아래 규칙 준수.

**A. Dev ➔ Test (QA 배포)**

- **Title:** `Deploy: Dev to Test (YYYY-MM-DD)`
- **Pre-requisite:** 머지 전 `pubspec.yaml`의 `version`을 올려야 합니다.
- **Issue:** 연결하지 않음

**B. Test ➔ Prod (정식 배포)**

- **Title:** `Release: v1.2.0` (버전명과 일치)
- **~~Action:** 머지되면 **GitHub Action이 자동으로 태그(v x.y.z)를 Push** 하고, 
**Jenkins가 배포**를 수행.~~

---

## **4. 🎨 Label Reference**

아래 라벨들이 릴리즈 노트 자동 분류에 사용.

| **Label Name** | **설명** | **릴리즈 노트 섹션** |
| --- | --- | --- |
| `Type: Feature` | 새로운 기능 | ✨ New Features |
| `Type: Bug` | 버그 수정 | 🐛 Bug Fixes |
| `Type: Design` | 디자인 변경 | 🎨 Design & UI |
| `Type: Refactor` | 리팩토링 | ♻️ Refactoring |
| `Type: Docs` | 문서 작업 | 📝 Documentation |
| `Area: Design` | 디자인 영역 표시 | (Type과 조합 사용) |

---

## **5. ⚡ Automation Summary**

1가지 자동화 봇이 있음.

1. **Release Drafter:** `main` 브랜치 머지 시 `pubspec.yaml` 버전을 읽어 **릴리즈 노트를 자동 작성**.
2. **~~Auto Tagging:** `main` 브랜치 머지 시 `pubspec.yaml` 버전을 읽어 **Git Tag를 자동 Push** 하고 **Jenkins 배포**를 트리거.~~
